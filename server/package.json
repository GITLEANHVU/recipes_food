{
  "name": "server",
  "version": "1.0.0",
  "description": "Fake API for project app",
  "main": "index.js",
  "scripts": {
    "json:server": "json-server --watch db.json --port 2021 --static ./public"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
